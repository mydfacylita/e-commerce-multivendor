# ========================================
# CONFIGURAÇÃO MÍNIMA - MYDSHOP
# ========================================
# As chaves de API são gerenciadas pelo painel administrativo 
# e salvas no banco de dados. Este arquivo contém apenas 
# configurações de infraestrutura.

# Banco de Dados MySQL (OBRIGATÓRIO)
# Substitua usuario, senha e ecommerce pelos valores corretos
DATABASE_URL="mysql://usuario:senha@localhost:3306/ecommerce"

# NextAuth - Autenticação (OBRIGATÓRIO)
# Gere com: openssl rand -base64 32
NEXTAUTH_SECRET="gere-uma-chave-secreta-com-minimo-32-caracteres"
NEXTAUTH_URL="https://mydshop.com.br"

# URL Pública (OBRIGATÓRIO)
NEXT_PUBLIC_SITE_URL="https://mydshop.com.br"

# Ambiente (OBRIGATÓRIO)
NODE_ENV="production"

# ========================================
# SEGURANÇA FINANCEIRA (OBRIGATÓRIO para transações)
# ========================================
# Chaves para criptografia de transações financeiras
# Gere com: openssl rand -hex 32
TRANSACTION_ENCRYPTION_KEY="gere-uma-chave-hex-com-64-caracteres"
TRANSACTION_SIGNING_KEY="gere-outra-chave-hex-com-64-caracteres"

# ========================================
# SEGURANÇA DE APIS (RECOMENDADO para produção)
# ========================================
# Secret para proteger jobs de CRON contra acesso não autorizado
# Gere com: openssl rand -hex 32
CRON_SECRET="gere-uma-chave-secreta-para-cron-jobs"

# API Key do app móvel (opcional - pode usar DB)
# APP_API_KEY="myd_..."

# ========================================
# WHATSAPP BUSINESS API
# ========================================
# Token de verificação para o webhook do WhatsApp (Meta)
WHATSAPP_VERIFY_TOKEN="MYDSHOP_WHATSAPP_VERIFY_2026"

# ========================================
# INTEGRAÇÕES (Configure no Painel Admin)
# ========================================
# ✅ Mercado Pago → /admin/pagamentos
# ✅ Mercado Livre → /admin/integracao/mercadolivre
# ✅ AliExpress → /admin/integracao/aliexpress
# ✅ Correios → /admin/configuracoes/frete
# ✅ WhatsApp → /admin/integracao/whatsapp
#
# Todas as chaves são salvas nas tabelas:
# - PaymentGateway (Mercado Pago)
# - SystemConfig (outras integrações)
